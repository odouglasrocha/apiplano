PORT=5000
MONGO_URI=mongodb://localhost:27017/sigp
MONGO_URI_INTERMEDIARIO=mongodb://localhost:27017/test

# SMTP configuration (backend only, never exposed to frontend)
SMTP_HOST=smtp.seu-servidor.com
SMTP_PORT=587
SMTP_USER=usuario@smtp.com
SMTP_PASS=sua_senha_segura
SMTP_SECURE=false
EMAIL_FROM="SIGP <no-reply@sigp.local>"

# Contatos visíveis no frontend (opcional):
# Preencha para exibir no rodapé da aplicação
VITE_CONTACT_NAME="orlando.rocha@pepsico.com"
VITE_CONTACT_EMAIL="orlando.rocha@pepsico.com"

# Auth for protected endpoints
JWT_SECRET=troque-por-uma-chave-secreta-forte
ADMIN_USER=admin
ADMIN_PASS=senha-admin-segura

# Recipients encryption key: 32 bytes base64 (e.g. openssl rand -base64 32)
RECIPIENTS_SECRET=troque-por-uma-chave-base64-de-32-bytes

# Optional app url for CORS
APP_URL=http://localhost:5173

# Teams Incoming Webhook (para enviar mensagem ao canal)
# Coloque aqui a URL gerada no conector do canal do Microsoft Teams
# Exemplo (NÃO use valores reais neste arquivo):
# TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/SEU-WEBHOOK
TEAMS_WEBHOOK_URL=

# URL pública do sistema (exibida no rodapé do relatório em e-mail e Teams)
SYSTEM_URL=https://planing-ita.com/

# URL pública da logo (PNG) para exibição no cartão do Teams.
# Se fornecer uma URL acessível publicamente, o relatório do Teams exibirá a logo como heroImage.
# Exemplo: https://seu-dominio.com/assets/logo-motor.png
LOGO_PUBLIC_URL=
# LOGO_PUBLIC_URL=https://planing-ita.com/assets/logo-motor.png

# BASE URL pública para servir imagens geradas do relatório (para o Teams)
# Deve apontar para o host/porta onde o backend está acessível externamente.
# Em ambiente local, use: http://localhost:5000
# Em produção, use seu domínio público (ex.: https://planing-ita.com)
PUBLIC_BASE_URL=http://localhost:5000

# Caminho para um navegador Chrome/Edge instalado localmente (opcional).
# Ajuda o Puppeteer a abrir uma página em ambientes restritos.
# Exemplos:
# CHROME_EXECUTABLE=C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe
# CHROME_EXECUTABLE=C:\\Program Files (x86)\\Microsoft\\Edge\\Application\\msedge.exe
CHROME_EXECUTABLE=

# Controla se o backend deve gerar uma imagem PNG do relatório para o Teams.
# Em ambientes com restrição a Puppeteer/Chromium (ex.: Windows corporativo/OneDrive), mantenha 'false' para estabilidade.
# Quando PUBLIC_BASE_URL estiver acessível publicamente (ngrok/localtunnel/cloudflared ou domínio), você pode mudar para 'true' para layout idêntico ao e-mail.
TEAMS_USE_IMAGE=false